Node:
(type) c
(layer)0
	Element:body
			Node:
			(type) h1
			(layer)1
				Element:h1
					Auth
			Node:
			(type) c
			(layer)1
				Element:p
					The Auth API offers some limited endpoints for checking whether an API key is valid and for confirming which scopes it has enabled.
