Node:
(type) c
(layer)0
	Element:body
			Node:
			(type) h1
			(layer)1
				Element:h1
					Account
			Node:
			(type) c
			(layer)1
				Element:span
				Element:a
					Run in Postman
				Element:span
					Postman is a useful developer tool for experimenting with and managing APIs that you work with
			Node:
			(type) c
			(layer)1
				Element:p
					The Account API allows you to perform administrative actions against GoSquared accounts. This includes actions like changing settings, configuration, and listing resources under the account.
			Node:
			(type) c
			(layer)1
						Node:
						(type) c
						(layer)2
							Element:h2
								Alert Preferences
						Node:
						(type) c
						(layer)2
							Element:p
								GoSquared's real-time alerting can be configured using the API. Change alert conditions and configure which services such as Twitter and Email to receive notifications.
			Node:
			(type) c
			(layer)1
						Node:
						(type) c
						(layer)2
							Element:h2
								Ignored Visitors
						Node:
						(type) c
						(layer)2
							Element:p
								GoSquared can prevent specific visitors from being tracked by adding them to the ignore list. With the ignoredVisitors function you can manage the visitor IDs in this list.
			Node:
			(type) c
			(layer)1
						Node:
						(type) c
						(layer)2
							Element:h2
								Report Preferences
						Node:
						(type) c
						(layer)2
							Element:p
								Periodic analytics reports sent via email can be controlled using the reportPreferences function.
			Node:
			(type) c
			(layer)1
						Node:
						(type) c
						(layer)2
							Element:h2
								Sites
						Node:
						(type) c
						(layer)2
							Element:p
								Obtain a full list of sites for an account.
