Node:
(type) c
(layer)0
	Element:article
			Node:
			(type) h1
			(layer)1
				Element:h1
					Client API Reference
				Element:a
					¶
			Node:
			(type) c
			(layer)1
				Element:p
					The Client APIs consists of REST APIs and Websocket APIs. These APIs are intended for use
				Element:strong
					exclusively
					for accessing an individual user’s data. This is useful, for example, if you are building a Desktop Application and want the user logged into that app to access their Messages and Conversations.
			Node:
			(type) c
			(layer)1
				Element:blockquote
				Element:p
				Element:strong
					Note
				Element:p
					If your goal is to interact with
				Element:em
					all
					of your Application’s data, then use the
				Element:a
					Server APIs
					instead. Client APIs exist solely for accessing an authenticated user’s data.
			Node:
			(type) c
			(layer)1
				Element:p
					This document is organized into the following sections:
			Node:
			(type) c
			(layer)1
				Element:ol
				Element:li
					Core concepts of working with Layer
				Element:li
					Common rules and expectations for working with REST and Websocket APIs
				Element:li
					Definition of the Objects used throughout the API
				Element:li
					Documentation for each endpoint or operation
			Node:
			(type) c
			(layer)1
				Element:a
					Concepts
